---
title: 'Slide Demo'
slide: true
theme: cloudflare
---

# Cloudflare Workers + Hono

スライドモードのデモ

- `slide: true` でスライド化
- `theme: cloudflare` でテーマ指定

---

## 区切りは `---`

本文中の `---`（水平線）でスライドを分割します。

slidev / reveal.js と同じ感覚で書けます。

---

## コードもOK

```ts
import { Hono } from 'hono'

const app = new Hono()
app.get('/', (c) => c.text('Hello Cloudflare!'))

export default app
```

ハイライトもそのまま効きます。

---

## 左右2カラム

左側に箇条書き

- Workers
- Hono
- D1 / KV / R2

::right::

![Hono](https://hono.dev/images/logo.png)

slidev と同じく `::right::` で区切るだけ。見出しは上に残ります。

---

## 操作方法

- `←` `→` / `space`: ページ移動
- スマホ: 左右スワイプ
- `f`: フルスクリーン
